[build]
  publish = "public"
  command = "cd netlify/functions && npm init -y && npm install @netlify/blobs && cd ../.."

[functions]
  directory = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
